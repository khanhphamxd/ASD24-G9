package model;

import java.util.ArrayList;

public class CustomerDAO {
    private ArrayList<Customer> customers;

    // Database connection can be created here (add customers to database, retrieve all customers)
    // It should also have a "permission" tag to separate customers from staffs
    // Login validation & check for unique username
    // NOTE: For any customer construction call for data extraction, please include the ID in the parameter of the constructor so that it won't mess with my ID tracker

}
